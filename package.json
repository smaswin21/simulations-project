{
  "name": "simulation-project",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"uvicorn backend.main:app --reload\" \"npm run dev --prefix sim-engine\" \"npm run dev --prefix frontend\"",
    "backend": "uvicorn backend.main:app --reload",
    "sim-engine": "npm run dev --prefix sim-engine",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
